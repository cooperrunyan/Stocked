scripts:
  local: deno run --allow-net --allow-env --allow-read --no-check src/server/server.ts
  dev: denon run --allow-net --allow-env --allow-read --no-check src/server/server.ts
  docker: docker-compose up --build
